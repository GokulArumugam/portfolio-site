import ArchitectureDiagram from "@/components/ArchitectureDiagram";
import ReconDemo from "@/components/ReconDemo";

export const metadata = {
  title: "Payments Reconciliation Engine",
  description: "A deterministic, SQL-first reconciler for switch-ledger attempts and T+1 bank settlements.",
  metric: "1M pairs · <5s",
  kind: "project",
};

# Payments Reconciliation Engine

A deterministic reconciliation engine for the two views of payment traffic that matter at daily close: what the switch says happened, and what the bank says settled.

**In plain terms:** Every digital payment leaves two records — one in the payment system that processed it, one in the bank file that settles the money a day later. They should match. In reality some payments go missing, get posted twice, or settle for slightly different amounts. This engine compares millions of records from both sides, finds every disagreement, and labels each one with evidence an operations team can act on — in under five seconds for a million pairs.

## Problem

The switch ledger is real-time and authoritative for payment attempts. The bank settlement file arrives at T+1 and is authoritative for money movement. They should describe the same transactions, but real operations create drops, duplicate posts, fee or rounding differences, status disagreements, late settlements, and malformed references. The job is not to make the totals look plausible: it is to classify every disagreement with evidence that an operator can audit.

## Architecture

<ArchitectureDiagram
  ariaLabel="Payments reconciliation architecture"
  adrBase="https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/"
  showReplay={false}
  nodes={[
    { id: "generator", title: "Seeded generator", subtitle: "two ledgers + manifest", x: 130, y: 250, what: "A seeded synthetic generator emits the switch ledger, the T+1 bank file, and a separate defect manifest describing every injected disagreement.", why: "Synthetic data exercises real payment failure patterns without touching production data, and the seed makes every run reproducible.", failure: "If generation and evaluation shared state, recall numbers would be circular — the engine never reads the manifest.", adr: "0003-ground-truth-oracle.md" },
    { id: "switch", title: "Switch ledger", subtitle: "real-time attempts", x: 365, y: 125, what: "The real-time view: every payment attempt the switch processed, with status, amount, and reference.", why: "It is authoritative for attempts, not for money movement, so it anchors one side of every comparison.", failure: "Duplicate posts and malformed references originate here and must be excluded before exact matching.", adr: "0001-deterministic-multipass-matching.md" },
    { id: "bank", title: "T+1 bank file", subtitle: "settled money", x: 365, y: 375, what: "The bank's settlement file arrives a day later and is authoritative for money that actually moved.", why: "Late, fee-adjusted, and occasionally missing records are the norm — the settlement file is where real-world mess enters.", failure: "Late settlements and rounding differences are expected inputs, not errors; the engine classifies them instead of rejecting them.", adr: "0001-deterministic-multipass-matching.md" },
    { id: "passes", title: "DuckDB SQL passes", subtitle: "5 numbered passes", x: 600, y: 250, what: "Five numbered DuckDB SQL passes: normalize, exclude repeated keys, exact match, constrained fuzzy rescue, classify residuals.", why: "Plain SQL keeps every decision auditable — an operator can read the exact rule that produced any match or exception.", failure: "The fuzzy pass is deliberately constrained; an over-eager matcher would hide real defects as false matches.", adr: "0002-duckdb-sql-core.md" },
    { id: "results", title: "Matches + exceptions", subtitle: "evidence out", x: 800, y: 250, what: "Every record pair ends up either matched or in a classified exception bucket with the evidence for the decision.", why: "Daily close needs zero unexplained records, not plausible-looking totals.", failure: "Verification compares engine output against the withheld manifest: 100% recall and zero false positives at 1M pairs.", adr: "0003-ground-truth-oracle.md" },
  ]}
  edges={[
    { source: "generator", target: "switch", kind: "main" },
    { source: "generator", target: "bank", kind: "main" },
    { source: "switch", target: "passes", kind: "main" },
    { source: "bank", target: "passes", kind: "main" },
    { source: "passes", target: "results", kind: "main" },
  ]}
  mainPaths={[["generator", "switch", "passes", "results"], ["generator", "bank", "passes", "results"]]}
/>

The generator produces the two ledger views plus a separate defect manifest. The engine never reads that manifest: five numbered DuckDB passes normalize inputs, exclude repeated keys, perform exact matching, attempt a deliberately constrained fuzzy rescue, then classify every residual. The manifest is used only after the run to verify what the engine found.

## Key decisions

1. **[ADR-0001: deterministic multi-pass matching](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0001-deterministic-multipass-matching.md).** Ordered rule passes make every result explainable. If constrained fuzzy matching has more than one candidate, it emits `AMBIGUOUS`; it never guesses.
2. **[ADR-0002: DuckDB SQL as the core](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0002-duckdb-sql-core.md).** The numbered SQL files are the matching logic, while orchestration only loads inputs and writes outputs. That makes the same rules runnable on a laptop, in CI, and in DuckDB-WASM.
3. **[ADR-0003: ground-truth oracle](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0003-ground-truth-oracle.md).** The generator emits a defect manifest separately from the reconciler inputs. Verification compares discovered `(txn_id, defect_class)` pairs with that oracle instead of relying on a small hand-written fixture set.

## Correctness contract

The generator emits a defect manifest that acts as the oracle. It is deliberately isolated from `switch_ledger` and `bank_settlement`, then compared with the reconciler outputs after the run. The contract is strict: every injected defect must be rediscovered, and no clean transaction may be reported as a defect.

Verified on a laptop: **1M pairs reconciled in 4.7s**, with **100% recall across all 35,000 injected defects** in the seven defect classes and **zero false positives**. The 5M-scale measurement is **5M pairs in 217s on the same laptop (memory-bound alongside the streaming pipeline VM; 175,000 defects, still 100% recall, zero false positives)**.

## Failure modes

| Failure mode | Engine behavior |
| --- | --- |
| Dedup on both sides | A repeated UTR on either or both sides is classified as `DUPLICATE` and excluded from later matching passes, preventing double matching. |
| `AMBIGUOUS` fuzzy candidates | More than one constrained candidate becomes an `AMBIGUOUS` exception. The engine must never guess. |
| Whole-file-missing | An empty bank file causes all remaining switch transactions to classify as `MISSING_IN_BANK`; the batch summary can raise a file-level completeness alarm. |

## Try it

Move the sliders to inject disjoint, seeded defects into 3,000 transaction pairs. Your browser runs the engine's actual, unmodified `sql/` files fetched from `public/recon-sql/`; only the CLI's input/output templates are resolved in memory so DuckDB-WASM can use its generated tables.

<ReconDemo />
