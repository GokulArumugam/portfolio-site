import ArchitectureDiagram from "@/components/ArchitectureDiagram";
import ReconDemo from "@/components/ReconDemo";

export const metadata = {
  title: "Payments Reconciliation Engine",
  description: "A deterministic, SQL-first reconciler for switch-ledger attempts and T+1 bank settlements.",
  metric: "1M pairs · <5s",
  kind: "project",
};

# Payments Reconciliation Engine

Every digital payment leaves two paper trails: one in the switch (the system that processed the attempt) and one in the bank file (that settles the actual money a day later). They should match. Sometimes they don't. This engine finds every mismatch — across a million records — in under five seconds.

**In plain terms:** think of it as the accountant who checks your wallet against your bank statement every single day, except your wallet has a million entries and the bank statement refuses to arrive on time. Payments go missing, get recorded twice, or settle for slightly different amounts. This engine compares both sides, flags every disagreement, and — this is the important part — explains exactly why it flagged each one, with evidence.

## Problem

The switch ledger is real-time and authoritative for payment attempts. The bank settlement file arrives at T+1 and is authoritative for money actually moving. In a perfect world they'd tell the same story. In the real world: payments get dropped, posted twice, settle late, settle with fee deductions, or arrive with reference numbers that look like a cat walked over the keyboard.

The job is not to make totals look plausible — anyone can make totals look plausible. The job is to classify every single disagreement with evidence an operations team can act on. No guessing allowed.

## Architecture

<ArchitectureDiagram
  ariaLabel="Payments reconciliation architecture"
  adrBase="https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/"
  showReplay={false}
  nodes={[
    { id: "generator", title: "Seeded generator", subtitle: "two ledgers + manifest", x: 130, y: 250, what: "A seeded synthetic generator emits the switch ledger, the T+1 bank file, and a separate defect manifest describing every injected disagreement.", why: "Synthetic data exercises real payment failure patterns without touching production data, and the seed makes every run reproducible.", failure: "If generation and evaluation shared state, recall numbers would be circular — the engine never reads the manifest.", adr: "0003-ground-truth-oracle.md" },
    { id: "switch", title: "Switch ledger", subtitle: "real-time attempts", x: 365, y: 125, what: "The real-time view: every payment attempt the switch processed, with status, amount, and reference.", why: "It is authoritative for attempts, not for money movement, so it anchors one side of every comparison.", failure: "Duplicate posts and malformed references originate here and must be excluded before exact matching.", adr: "0001-deterministic-multipass-matching.md" },
    { id: "bank", title: "T+1 bank file", subtitle: "settled money", x: 365, y: 375, what: "The bank's settlement file arrives a day later and is authoritative for money that actually moved.", why: "Late, fee-adjusted, and occasionally missing records are the norm — the settlement file is where real-world mess enters.", failure: "Late settlements and rounding differences are expected inputs, not errors; the engine classifies them instead of rejecting them.", adr: "0001-deterministic-multipass-matching.md" },
    { id: "passes", title: "DuckDB SQL passes", subtitle: "5 numbered passes", x: 600, y: 250, what: "Five numbered DuckDB SQL passes: normalize, exclude repeated keys, exact match, constrained fuzzy rescue, classify residuals.", why: "Plain SQL keeps every decision auditable — an operator can read the exact rule that produced any match or exception.", failure: "The fuzzy pass is deliberately constrained; an over-eager matcher would hide real defects as false matches.", adr: "0002-duckdb-sql-core.md" },
    { id: "results", title: "Matches + exceptions", subtitle: "evidence out", x: 800, y: 250, what: "Every record pair ends up either matched or in a classified exception bucket with the evidence for the decision.", why: "Daily close needs zero unexplained records, not plausible-looking totals.", failure: "Verification compares engine output against the withheld manifest: 100% recall and zero false positives at 1M pairs.", adr: "0003-ground-truth-oracle.md" },
  ]}
  edges={[
    { source: "generator", target: "switch", kind: "main" },
    { source: "generator", target: "bank", kind: "main" },
    { source: "switch", target: "passes", kind: "main" },
    { source: "bank", target: "passes", kind: "main" },
    { source: "passes", target: "results", kind: "main" },
  ]}
  mainPaths={[["generator", "switch", "passes", "results"], ["generator", "bank", "passes", "results"]]}
/>

A generator creates both ledgers plus a secret answer sheet (the defect manifest — every injected mistake, written down). The engine never gets to peek at that answer sheet. It runs five numbered SQL passes: clean the data, set aside duplicates, match the easy exact ones, attempt a carefully limited fuzzy rescue for the mangled ones, and classify whatever's left. Only after the engine is done do we check its work against the answer sheet.

## Key decisions

1. **[ADR-0001: deterministic multi-pass matching](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0001-deterministic-multipass-matching.md).** Rules run in order, so every result is explainable. If the fuzzy matcher finds more than one possible match, it refuses to guess and raises `AMBIGUOUS`. A matcher that guesses is a matcher that quietly hides real problems.
2. **[ADR-0002: DuckDB SQL as the core](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0002-duckdb-sql-core.md).** The matching logic is just SQL files you can read top to bottom. No framework, no magic. The same SQL runs on my laptop, in CI, and in your browser via DuckDB-WASM. That portability is why the demo below exists.
3. **[ADR-0003: ground-truth oracle](https://github.com/GokulArumugam/payments-reconciliation/blob/main/docs/adr/0003-ground-truth-oracle.md).** The generator writes the defect manifest separately from the inputs the engine reads. Verification compares what was injected with what was found. Anything less would be grading your own exam.

## Correctness contract

The contract is strict and a little brutal: every injected defect must be found, and no innocent transaction may be accused. Recall of 100%, false positives of zero. Anything less and the build fails.

Verified on a laptop: **1M pairs reconciled in 4.7s**, with **100% recall across all 35,000 injected defects** in the seven defect classes and **zero false positives**. Scaled it up too: **5M pairs in 217s on the same laptop** (memory-bound because a streaming pipeline VM was hogging the machine — rude — still 100% recall, zero false positives).

## Failure modes

| Failure mode | Engine behavior |
| --- | --- |
| Dedup on both sides | A repeated UTR on either or both sides is classified as `DUPLICATE` and excluded from later matching passes, preventing double matching. |
| `AMBIGUOUS` fuzzy candidates | More than one constrained candidate becomes an `AMBIGUOUS` exception. The engine must never guess. |
| Whole-file-missing | An empty bank file causes all remaining switch transactions to classify as `MISSING_IN_BANK`; the batch summary can raise a file-level completeness alarm. |

## Try it

Play with the sliders below — inject defects into 3,000 transaction pairs and watch the engine catch them. Your browser runs the engine's actual, unmodified SQL files; nothing is faked for the demo. Go on, try to fool it. It doesn't get tired, and it doesn't guess.

<ReconDemo />
